{
  "kind": "build_request",
  "title": "Revert to a stable working version",
  "priority": "high",
  "requirements": [
    {
      "id": "REQ-1",
      "text": "Revert the application to the last known stable state before the recent changes that caused issues. Restore both frontend and backend code to the previously working version, ensuring the app builds and deploys successfully.",
      "target": "both",
      "source": {
        "messageIds": [
          "msg-latest"
        ],
        "quotes": [
          "it is not reverted"
        ]
      },
      "acceptanceCriteria": [
        "The application builds without errors",
        "The application deploys successfully to the network",
        "All previously working features (registration, gallery, admin dashboard, CMS content) function correctly",
        "The admin login and dashboard data loading work as expected"
      ]
    }
  ],
  "constraints": [
    "Do not introduce new features or UI changes — only restore stable functionality",
    "Preserve the existing data model and backend state"
  ],
  "nonGoals": [
    "Adding new features",
    "Redesigning any UI components",
    "Changing the authentication mechanism"
  ],
  "imageRequirements": {
    "required": [],
    "edits": []
  }
}